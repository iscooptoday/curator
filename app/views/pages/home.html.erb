
     <div>
      
    <% if user_signed_in? %>

    
  <div>
  <%= form_tag search_path, method: 'get'  %>
  <%= text_field_tag :search, params[:search] %>
  <%= submit_tag "Search", name: nil %>

</div>







    <%# showing all the tags each showing boldness according to frequency the styling is in topics.css.scss%>
    <h3> POPULAR CATEGORIES</h3>
    <div id="tag_cloud">
     <% tag_cloud Topic.tag_counts, %w{s m l} do |tag, css_class| %>
     <%= link_to tag.name, tag_path(tag.name), class: css_class %>
     <% end %>
   </div><br><br><br><br>


                  

    <h3> MY SUBSCRIPTIONS</h3>
    <% current_user.followees(Topic).each do |topic| %>
    <%= link_to topic.description, topic_path(topic) %> by 
    <%= topic.author if topic.author  %><br> published <b style="color:red"><%= topic.frequency if topic.frequency %></b></p>
  
  <% end %>        
  <% else %>
     
  <p>Iscoop lets you create a personal newsletter that your friends and 
    family can subscribe to.<br> You can share anything in your newsletter. you can upload photos and embed anything you come across on the internet that you like<br>Why a personal newsletter? a lot of the stuff you post on social media gets seen by a small number of your fiends. Having a personal newsletter guarantees that all your subscribers will get anything you share because they will get a text message anytime you publish something in you personal newsletter </p> 
      
  <%= link_to "Log in", new_user_session_path, class: "btn btn-default btn-lg" %>
  <%= link_to "Sign up", new_user_registration_path, class: "btn btn-default btn-lg" %><br><br>
  <% end %> 
  
  </div>  

