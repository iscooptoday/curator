<%= form_for(@link_attachment) do |f| %>
  <% if @link_attachment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@link_attachment.errors.count, "error") %> prohibited this link_attachment from being saved:</h2>

      <ul>
      <% @link_attachment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :link_id %><br>
    <%= f.number_field :link_id %>
  </div>
  <div class="field">
    <%= f.label :avatar %><br>
    <%= f.text_field :avatar %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
