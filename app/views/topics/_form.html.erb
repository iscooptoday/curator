

 



<%= form_for(@topic) do |f| %>
  <% if @topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@topic.errors.count, "error") %> prohibited this topic from being saved:</h2>

      <ul>
      <% @topic.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field :description, class: "form-control",placeholder:" what do you want to call it",:autofocus => true %>
  </div>

  <div class="form-group">
       <%= f.label :frequency %>
       <%= f.text_field :frequency, class: "form-control", placeholder:"Let your them know how often you will be sharing..dily, weekly, it depends " ,:autofocus => true %>
     </div>

  <div class="form-group">
    <%= f.label :created_by %>
    <%= f.text_field :author, class: "form-control",placeholder:"dosent have to be your real name if u dnt want to",:autofocus => true %>
  </div>

  <div class="form-group">
    <%= f.label :Welcome_Message_for_followers %>
    <%= f.text_field :welcome_message, class: "form-control",placeholder:"eg hello and welcome to my newsletter. ",:autofocus => true %>
  </div>


  
  
  <div class="actions">
    <%= f.submit "CREATE NEWSLETTER", class: "btn btn-primary" %>
  </div>
<% end %>


