   
    <div class="container">
    
    <% @topics.each do |topic| %>   
    <div class="topics">
    <ul> 
        <li> <%= link_to topic.description, topic_path(topic) %> by
            <%= topic.author if topic.author %>
           <p> <%= topic.welcome_message if topic.welcome_message %></p>
   </ul>
   </div>   
   <% end %>  

   





